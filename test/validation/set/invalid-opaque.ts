import {Test} from '../../types'
import { set } from '../../../src'



  {
    value: 'invalid',
    type: 'set',
    refinement: undefined,
    path: [],
    branch: [data],
  },
]

export const invalidOpaqueTest: Test = {
 Struct: set(),
 data: 'invalid',
 failures: [,
}
